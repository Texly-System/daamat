// Deprecated - use Target from ./base instead
// Kept for backwards compatibility

/**
 * TODO: Same here this need a redo but after the model has been defined
 * then the schema can be extracted correctly
**/

export type { Target as ModelTarget } from "./base";
