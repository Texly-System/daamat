# @damatjs/deps 1.0.6

- Added the pinned official `pgvector` 0.3.0 node-postgres adapter at
  `@damatjs/deps/pgvector`.
- Raised the Hono dependency floor to 4.12.34 so consumers cannot resolve the
  vulnerable releases rejected by the repository security gate.
