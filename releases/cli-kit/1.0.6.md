# @damatjs/cli-kit 1.0.6

> Kit installation preserves repeated capability target overrides.

## Fixed

- `kit plan`, `add`, and `update` mark `--target capability=path` repeatable so
  every supplied destination reaches the shared installer.

## References

- Current behavior: [package README](../../packages/cli/kit/README.md)
- Internals: [Kit internals](../../packages/cli/kit/docs/README.md)
